/** Pastilles orbitales hero landing — source unique pour `HeroOrbitLabels`. */
export const HOME_HERO_ORBIT_LABELS = [
  "fait maison",
  "chaud maintenant",
  "table de quartier",
  "simple et humain",
] as const;

export type HeroOrbitLabelsTuple = readonly [string, string, string, string];
